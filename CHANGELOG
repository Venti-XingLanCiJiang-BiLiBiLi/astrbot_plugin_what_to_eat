## 更新日志

### v1.3.0

- ✨ 新增「初始化新会话菜单为默认菜单」开关（`init_from_default`）
  - 开启后新群/新用户首次使用插件时自动继承默认菜单菜品
  - 保存配置时自动将所有空菜单填充为默认菜单
- ✨ 新增「默认菜单变更时同步至所有菜单」开关（`sync_default_to_all`）
  - 开启后对默认菜单的添加操作自动同步至所有其他菜单
  - 保存配置时自动将默认菜单同步至所有菜单
- ✨ WebUI 设置页面新增群号/用户ID 自动补全搜索功能
  - 输入时动态匹配已存在的菜单会话，最多显示 6 条
  - 匹配关键词高亮显示
- ✨ WebUI 设置页面新增搜索 ID 的后端 API（`/menu/search_ids`）
- 🔧 修正 `@register` 装饰器的 author 字段为 `StarryLanMusic`
- 📝 更新 README 与 CHANGELOG 文档

### v1.1.0

- ✅ 符合 AstrBot 规范：数据存储到 `data/plugin_data/what_to_eat/` 目录
- ✅ 新增 WebUI 设置页面，支持可视化菜单管理
- ✅ 支持导入导出 JSON 数据
- ✅ 支持自定义回复模板（随机推荐、开席推荐）
- ✅ 支持配置开席推荐菜品数量
- ✅ 新增 8 个 Web API 接口供设置页面调用

### v1.0.1

- 初始版本发布